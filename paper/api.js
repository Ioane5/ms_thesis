

// API FOR Application

// Initialize Library
function initialize(String publicKey, String privateKey);

function send(String[] recepients, String data);

// Query methods will be added upon demand
function query(String key);

// Event to be fired
function onMessage(String message);

// Problems

1. No idea how to work with Multiple device account. 
2. 